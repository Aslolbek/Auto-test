<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Registratsiya</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #e0f7fa; /* Ochiq ko'k fon */
            color: #004d40; /* To'q yashil matn */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: #00796b; /* Yashil rang */
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 32px;
        }

        nav {
            background-color: #004d40; /* To'q yashil rang */
            padding: 12px;
            text-align: center;
        }

        nav a {
            color: white;
            padding: 12px 25px;
            text-decoration: none;
            font-size: 18px;
            margin: 0 15px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        nav a:hover {
            background-color: #00796b; /* Hover effektida yashil rang */
        }

        .form-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 50px;
        }

        .form-card {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }

        .form-card h3 {
            margin-bottom: 20px;
            color: #00796b; /* Yashil rang */
        }

        .form-card input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        .form-card button {
            width: 100%;
            padding: 12px;
            background-color: #00796b;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }

        .form-card button:hover {
            background-color: #004d40;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #00796b; /* Yashil rang */
            color: white;
            margin-top: auto;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header>
        Admin Panel - Registratsiya
    </header>

    <!-- Navbar -->
    <nav>
        <a href="/admin-page">Bosh sahifa</a>
        <a href="/user/all">Foydalanuvchilarni Ko'rish</a>
        <a href="/admin/subject/all">Mavzularni Ko'rish</a>
    </nav>

    <!-- Registratsiya Formasi -->
    <div class="form-container">
        <div class="form-card">
            <h3>Yangi Adminni Ro'yxatdan O'tkazish</h3>
            <form action="/register" method="POST">
                <input type="text" name="username" placeholder="Foydalanuvchi ismi" required>
                <input type="email" name="email" placeholder="Email manzili" required>
                <input type="password" name="password" placeholder="Parol" required>
                <input type="password" name="confirmPassword" placeholder="Parolni tasdiqlash" required>
                <button type="submit">Ro'yxatdan O'tish</button>
            </form>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>&copy; 2025 Admin Panel</p>
    </div>

</body>
</html>
